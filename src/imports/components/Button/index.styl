@import "~styles/config.styl"

.button
  outline none
  cursor pointer
  color $white
  padding 14px 40px
  font-size 16px
  border-radius 32px
  border 1px solid $blue
  background-color $blue

  &:disabled
    opacity 0.9
