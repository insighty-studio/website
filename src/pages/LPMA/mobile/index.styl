.lpma-page
  +iphone()
    .white-paper
      background linear-gradient(white, $snow)

      .paper-width .footer
        display block
        background-color transparent

    >.footer
      iphoneDisabled()
