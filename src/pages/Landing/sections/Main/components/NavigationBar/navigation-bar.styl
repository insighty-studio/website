@import '~styles/config.styl'

.navigation-bar
  display flex
  justify-content flex-end
  margin-top 32px

  .links a
    color $black
    font-size $small
    line-height 20px
    text-decoration none
    margin-right 32px
    cursor pointer

    &:last-child
      margin-right 0
