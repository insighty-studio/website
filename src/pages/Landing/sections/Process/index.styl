@import "~styles/config.styl"

.process
  display flex
  flex-direction column

  +iphone()
    margin: 64px 0
    max-width 100%
    width 100%

  .section-heading
    margin-top 120px

  .description
    display flex
    flex-direction column

    .row
      display flex
      justify-content space-around
      width 100%
      margin-top 86px

      +iphone()
        display: block
        margin-top 0

      &:first-child
        margin-top 96px

        +iphone()
          margin-top: 0
